import 'package:flutter/material.dart';

class MyWarningNotes extends StatelessWidget {
  const MyWarningNotes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
